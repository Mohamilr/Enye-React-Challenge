import React from 'react';

const Loader = () => {
    return (
        <div style={{textAlign: 'center', width: '100vw', height: '100vh'}}>
            <h1>Loading...</h1>
        </div>
    );
}

export default Loader;